<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/header.ejs') %>
    <link rel="stylesheet" href="../style/index.css">
    <link rel="stylesheet" href="../style/log-in-toast.css">
    <link rel="stylesheet" href="../style/carrucelPubli.css">
    <title>Agenda Verde</title>
</head>
<body>
    
    <%- include('partials/navigation.ejs') %>
    <div class="log-in-toast" id="btn-abiri-popup">
        <a href="#" id="btn-abiri-popup"> <img class="imglogoperfil" src="../../img/logoperfil.png"alt="logoperfil"></a>
    </div>

    <div class="bienvenida">
        <h2>Bienvenido</h2>
        <h1>AGENDA VERDE</h1>
        <img src="./img/logo.png" alt="logo">
    </div>

    <!-- carrusel de la publicidad de los otros trabajos -->
    <h2 class="tituloCarrucel">Otros proyectos en camino</h2>
    <%- include('partials/carrucelPubli.ejs') %>

    <!-- inicio de secion -->
    <div class="overlay" id="overlay">
        <div class="popup" id="popup">
            <a href="#" id="btn-cerrar-popup" class="btn-cerrar-popup"> <i class="bi bi-x"></i> </a>
            <h3>INICIAR SESION</h3>
            <form method="POST" action="/admin-page">
                <div class="contenedor-inputs">
                    <input id="usuario" type="text" name="usuario" placeholder="Usuario" required>
                    <input id="password" type="password" name="password" placeholder="Contraseña" required>
                </div>
                <input type="submit" class="btn-submit" value="Iniciar Sesion">
            </form>
        </div>
    </div>

    <%- include('partials/toast.ejs')%>

    
    <!-- <div class="container p-4">
        <div class="row">
            < ecoActividad.forEach(function (ecoActividad) { > en donde va <> estos simbolos va un simbolo de porcentaje dentro
                <div class="col-md-4">
                    <div class="card">
                        <div class="card-header">
                            <= ecoActividad.titulo > / <= ecoActividad.autor >
                        </div>
                        <div class="card-body">
                            <p><= ecoActividad.descripcion ></p>
                        </div>
                        <div class="card-footer">
                            <a href="/delete/<= ecoActividad.id >" class="btn btn-danger">
                                Eliminar
                            </a>
                        </div>
                    </div>
                </div>
                <div }) >
            </div>
        </div> -->
        <script>
            var btnAbrirPopup = document.getElementById('btn-abiri-popup');
            var overlay = document.getElementById('overlay');
            var popup = document.getElementById('popup');
            var btnCerrarPopup = document.getElementById('btn-cerrar-popup');

            btnAbrirPopup.addEventListener('click', function(){
                overlay.classList.add('active');
                popup.classList.add('active');
            });

            btnCerrarPopup.addEventListener('click', function(){
                overlay.classList.remove('active');
                popup.classList.add('active');
            })
        </script>
    </body>
    </html>